[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "command-ai"
version = "0.0.6"
requires-python = ">=3.7"
dependencies = [
    "openai>=0.27",
]

[project.scripts]
ai = "ai.ai:main"
